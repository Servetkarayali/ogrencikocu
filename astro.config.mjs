import { defineConfig } from 'astro/config';

export default defineConfig({
  // Alan adı belli olunca buraya yazılacak (SEO ve site haritası için)
  site: 'https://ogrencikocu.workers.dev',
  trailingSlash: 'ignore',
});
